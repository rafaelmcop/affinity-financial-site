(()=>{const path=location.pathname;if(!path.startsWith('/admin/')&&!path.startsWith('/agentes/'))return;let busy=false;const agent=path.startsWith('/agentes/');async function access(){try{const r=await fetch('/api/trpc/careers.access?input='+encodeURIComponent(JSON.stringify({json:{}})),{credentials:'include'});if(!r.ok)return null;return (await r.json())?.result?.data?.json||null}catch{return null}}function add(info){if(!info?.allowed||document.querySelector('[data-careers-menu]'))return;const labels=[...document.querySelectorAll('a,button')];const anchor=labels.find(el=>/usuários|usuarios/i.test(el.textContent||''))||labels.find(el=>/avaliações|avaliacoes/i.test(el.textContent||''))||labels.find(el=>/configurações|configuracoes/i.test(el.textContent||''));if(!anchor)return;const link=document.createElement('a');link.dataset.careersMenu='1';link.href='/candidaturas.html?portal='+(agent?'agent':'admin');link.className=anchor.className;link.style.textDecoration='none';link.innerHTML=`<span>Candidaturas</span>${Number(info.pendingCount)>0?`<b style="margin-left:auto;min-width:22px;padding:2px 6px;border-radius:99px;background:#ffb000;color:#000;font-size:11px;text-align:center">${Number(info.pendingCount)>99?'99+':Number(info.pendingCount)}</b>`:''}`;anchor.parentElement?.insertBefore(link,anchor.nextSibling)}access().then(info=>{add(info);new MutationObserver(()=>{if(busy)return;busy=true;requestAnimationFrame(()=>{add(info);busy=false})}).observe(document.body,{childList:true,subtree:true})})})();
